version: "3.9"

services:
  dataset-creator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dataset_creator
    volumes:
      - ./artifacts:/app/artifacts
      - ./data:/app/data
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
    command: [ "uv", "run", "create_dataset_fasttext.py" ]

volumes:
  artifacts:
